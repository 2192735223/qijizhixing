# 主页前端重设计 · 交接文件

> 本文件用于交接给另一位 AI 伙伴，合作重新设计「齐迹智行」项目的主页前端。
> 请务必先通读「已知问题与坑」和「硬性约束」两节，再动手。

---

## 1. 项目概览

- **项目名**：齐迹智行 · 齐文化 AR 互动展示
- **背景**：2026 年暑假社会实践项目，山东理工大学「齐迹智行」实践团
- **性质**：纯静态 H5 / WebAR 展示站，部署在 **GitHub Pages**
- **主页定位**：整个项目的入口页，聚合六大功能模块，用户扫码即可在手机上进入对应体验

---

## 2. 技术栈与部署

- **技术栈**：纯静态 HTML + CSS + 原生 JavaScript，**无框架、无构建工具**（这是硬约束，见第 9 节）
- **依赖库**：仅主页用到 `libs/qrcode.min.js`（本地文件，约 20KB）
- **部署方式**：GitHub Pages，仓库 `qijizhixing`，`master` 分支直接发布
  - 主页线上地址形如：`https://<用户名>.github.io/qijizhixing/`
- **本地预览**：项目根目录有 `serve.bat`，双击启动 `python -m http.server 8000`，访问 `http://localhost:8000`
  - ⚠️ 注意：**不要用 `file://` 直接打开**，否则二维码会显示「需 HTTP 服务」占位（代码里有判断）

---

## 3. 当前主页现状

- **文件位置**：`h5-webar/index.html`（单文件，约 6.9KB）
- **结构**（自上而下）：

| 区块 | 类名 | 说明 |
|------|------|------|
| 头部 | `header` | emoji 徽记 🏺 + 主标题「齐迹智行」+ 口号 + 副标题 + 金色分隔线 |
| 模块标题 | `.section-title` | 「扫码体验六大模块」 |
| 卡片网格 | `.grid` + `.card` | 6 张功能卡片，每张含图标 / 名称 / 徽标 / 二维码 / 提示 |
| 底部入口 | `.enter` | 提示文字 + 「进入 AR 青铜鼎体验」按钮 |
| 页脚 | `footer` | 资源来源 + 版权 |

- **每张卡片结构**（示例）：
  ```html
  <a class="card" href="ar-bronze/" target="_blank">
    <div class="icon">🏺</div>
    <div class="label">AR 青铜鼎</div>
    <div class="badge">WebAR · 真 AR</div>
    <div class="qr" id="qr0"></div>
    <div class="qrcode-hint">扫码进入 AR 体验</div>
  </a>
  ```

---

## 4. 六大功能模块清单（对应链接，勿改动）

| 图标 | 名称 | 链接（相对路径） | 类型 | 子页面状态 |
|------|------|------------------|------|-----------|
| 🏺 | AR 青铜鼎 | `ar-bronze/` | WebAR 真 AR | ✅ 已修复可用 |
| 🏯 | 齐国故城 | `gu-city/` | 3D 复原 | 待验证 |
| 📜 | 管仲改革 | `guanzhong/` | 时间轴 | 待验证 |
| 🎓 | 稷下学宫 | `jixia/` | 答题互动 | 待验证 |
| ⚽ | 蹴鞠 | `cuju/` | 点击互动 | 待验证 |
| 🏅 | 青春传承 | `youth/` | 电子证书 | 待验证 |

> 这些 `href` 是**相对路径**，重设计时必须保持不变，否则二维码和跳转都会失效。

---

## 5. 关键依赖与资源

- **二维码库**：`libs/qrcode.min.js`（本地，无需 CDN，很小）
- **图标**：全部使用 **emoji**（无图片资源，零额外加载）
- **AR 标识资源**：
  - `data/data/patt.hiro` — Hiro 标识 pattern 文件（AR.js 用）
  - `data/data/camera_para.dat` — 相机标定参数
  - `data/markers/hiro_border_p0.png` — 可打印的 Hiro 标识图
- **`qrcodes/` 目录**：目前是**空目录**（历史遗留），主页二维码是 JS 动态生成，不使用该目录

---

## 6. 二维码功能说明（重要，重设计必须保留）

二维码是主页的**核心功能**，逻辑如下（`index.html` 第 99–144 行）：

```javascript
var baseUrl = location.origin + location.pathname.replace(/\/[^/]*$/, "");
// 例：主页 https://xxx.github.io/qijizhixing/ 时，baseUrl = https://xxx.github.io/qijizhixing
var pages = [
  baseUrl + "/ar-bronze/",
  baseUrl + "/gu-city/",
  baseUrl + "/guanzhong/",
  baseUrl + "/jixia/",
  baseUrl + "/cuju/",
  baseUrl + "/youth/"
];
new QRCode(容器, { text: url, width: 80, height: 80, colorDark: "#1a1209", colorLight: "#ffffff" });
```

**关键点（务必遵守）：**

1. `qrcode.min.js` 的 `QRCode()` 构造函数**只接受 `<div>` 容器**，不接受 `<canvas>`。
   - 正确：`<div class="qr" id="qr0"></div>`，库会自动在 div 内生成 canvas/img。
   - 错误：`<canvas id="qr0">` 会导致二维码空白（此坑已踩过并修复）。
2. 二维码 URL 通过 `location.origin + location.pathname` 动态计算，**依赖主页路径结构**。若重设计后主页路径不变，此逻辑无需改动。
3. `file://` 协议下会走 fallback 提示「需 HTTP 服务」，这是**故意的**（本地必须起 HTTP 服务）。

---

## 7. 已知问题与坑（务必阅读）

1. **二维码必须用 `<div>` 容器**（见第 6 节），不要回退成 `<canvas>`。
2. **不要用 `file://` 打开**，二维码会显示占位提示。
3. **链接用相对路径**，不要改成绝对路径（否则换域名/换环境就崩）。
4. **子页面（尤其 `ar-bronze/`）刚经历过一轮深度修复**，涉及：
   - A-Frame / AR.js 脚本已移到 `<head>` 且必须在 `<a-scene>` 之前；
   - 自定义组件必须在 `<a-scene>` 之前注册；
   - 大资源（aframe、模型）已改用 jsDelivr CDN 加速。
   - **重设计主页时不要动子页面**，保持 `href` 相对路径不变即可。

---

## 8. 重设计目标与范围

- **范围**：仅 `h5-webar/index.html`（主页），**不涉及**六大子页面。
- **可自由发挥的部分**：
  - 视觉风格、布局、动效、卡片设计
  - 头部 / 页脚的文案与排版
  - 是否增加 banner、模块介绍、过渡动画等
- **建议方向**：移动端优先、突出「扫码体验」这一核心交互、对齐齐文化 / 青铜器的庄重历史感。

---

## 9. 硬性约束（不可破坏）

1. **保持纯静态**：无框架、无构建、无服务端依赖（GitHub Pages 限制）。
2. **保留二维码功能**：六大模块的扫码入口必须可用。
3. **保持链接结构**：`href` 相对路径、`id="qr0"` ~ `id="qr5"`、`pages` 数组顺序一一对应。
4. **保持二维码生成方式**：`QRCode()` 传入 `<div>` 容器。
5. **移动端兼容**：保留 `viewport` 的 `user-scalable=no`（或等价移动端适配）。

---

## 10. 当前配色主题（参考，可继承也可重新设计）

| 用途 | 色值 |
|------|------|
| 页面背景 | `#1a1209`（深棕黑） |
| 背景渐变高光 | `#3d2b1a` |
| 文字主色 | `#f0e6d3`（米白） |
| 金色主强调 | `#b8860b` |
| 亮金 | `#e8c97a` |
| 暗金文字 | `#c4a970` |
| 灰金 | `#8a7a60` |
| 深灰金 | `#6a5a40` |
| 卡片背景 | `linear-gradient(145deg,#2a1f12,#1e160c)` |
| 边框 | `#4a3820` |

- 字体栈：`-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`
- 整体风格：暗色 + 金色点缀，营造青铜器 / 齐文化的历史厚重感。

---

## 11. 交付期望

重设计完成后，请产出：
1. 新的 `index.html`（可直接替换现有文件）；
2. 如需额外资源（图片 / CSS / JS），请说明文件路径与用途；
3. 简要说明改动了什么、保留了哪些原功能。

> 建议重设计后先在本地用 `serve.bat`（`http://localhost:8000`）验证：二维码正常显示、六张卡片跳转正常、移动端适配无横向滚动。
